[TEMPLATE]
# Copy to .env if you need to override defaults.
# Defaults (dev): API is proxied via Vite (/api -> http://localhost:8000),
# WebSocket is proxied (/ws -> ws://localhost:8000). Only set these when
# pointing at a deployed backend directly.
VITE_API_URL=/api/v1
